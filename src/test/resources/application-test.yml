spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ecommerce_test
    username: myuser
    password: secret
    driver-class-name: com.mysql.cj.jdbc.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: validate

jwt:
  secret: test-secret-key-for-hmac-signing-must-be-long-enough
  expiration: 3600000
