# ECommerceAPI 작업 지침

## 프로젝트 개요

ECommerceAPI는 사용자 인증, 상품 관리, 장바구니, 주문, 결제 상태 전이를 제공하는 Spring Boot 기반 REST API다. 현재 결제는 외부 PG 연동 없이 API로 결과를 시뮬레이션한다.

공식 프로젝트 사실은 코드, Flyway 마이그레이션, 이 문서가 연결하는 `docs/` 문서를 기준으로 판단한다. 개인 메모인 `local-notes/`는 사용자가 명시적으로 요청하지 않는 한 읽거나 프로젝트 사실의 근거로 사용하지 않는다.

## 저장소 구조

- `src/main/java/.../domain`: 사용자, 상품, 장바구니, 주문, 결제 도메인
- `src/main/java/.../security`, `config`: JWT 인증과 Spring Security 설정
- `src/main/java/.../common`: 공통 응답 및 예외 처리
- `src/main/resources/db/migration`: Flyway 데이터베이스 스키마
- `src/test`: 단위 및 MVC 슬라이스 테스트
- `docs/domain`: 도메인별 규칙과 불변조건
- `docs/decisions`: 코드만으로 드러나지 않는 주요 설계 결정

## 기본 명령

Windows:

```powershell
.\gradlew.bat test
.\gradlew.bat build
.\gradlew.bat bootRun
docker compose up -d
docker compose down
```

macOS/Linux:

```shell
./gradlew test
./gradlew build
./gradlew bootRun
docker compose up -d
docker compose down
```

Java 21과 로컬 MySQL이 필요하다. 상세 절차는 `docs/development.md`를 따른다.

## 작업별 공식 문서

- 제품 목적, 현재 범위, 비범위: `docs/product.md`
- 전체 구조, 주요 흐름, 데이터 모델, 트랜잭션, 보안: `docs/architecture.md`
- 도메인 규칙: `docs/domain/*.md`
- 설계 결정: `docs/decisions/README.md`
- 로컬 개발과 검증: `docs/development.md`
- 현재 미구현 사항과 제약: `docs/known-limitations.md`

문서와 코드가 다르면 먼저 차이를 확인한다. 확정된 현재 동작은 코드와 Flyway를 기준으로 문서를 갱신하되, 의도적인 정책 변경은 관련 ADR도 함께 검토한다.

## 아키텍처 및 코딩 제약

- 기존 패키지 기반 계층형 모놀리스 구조를 기본으로 유지한다.
- HTTP 계층은 요청 검증과 응답 변환을 담당하고, 트랜잭션과 비즈니스 흐름은 서비스 계층에 둔다.
- 도메인 상태 전이와 값 자체의 불변조건은 가능한 한 엔티티가 보장한다.
- 영속 스키마 변경은 새 Flyway 마이그레이션으로 수행한다. 적용된 마이그레이션을 소급 수정하지 않는다.
- 리소스 조회에는 인증 사용자 ID를 포함해 소유권을 보장한다.
- 외부 시스템이 추가되면 도메인 서비스가 공급자 SDK에 직접 결합되지 않도록 명시적인 포트와 어댑터 경계를 둔다.

## 반드시 보존할 불변조건

- 상품 가격은 양수이며 재고는 음수가 될 수 없다.
- 장바구니 수량과 주문 수량은 1 이상 99 이하다.
- 사용자는 하나의 장바구니만 가지며 같은 상품은 장바구니에서 중복 행이 될 수 없다.
- 주문은 상품명과 가격을 주문 시점 스냅샷으로 보존한다.
- 주문 생성은 재고를 차감하지 않는다. 결제 완료 트랜잭션에서 재고 차감, 결제 성공, 주문 `PAID` 전이를 함께 수행한다.
- 주문·장바구니·결제는 소유자 범위로 조회하고 변경한다.
- 같은 주문에는 `PENDING` 결제가 하나만 존재할 수 있다.
- 비밀번호는 BCrypt로 암호화해 저장하며 실제 카드 정보는 저장하지 않는다.

## 사람의 사전 승인이 필요한 변경

- 데이터 삭제, 컬럼 제거 등 복구가 어려운 데이터베이스 마이그레이션
- 인증·인가 정책을 완화하거나 공개 엔드포인트 범위를 바꾸는 작업
- 재고 차감 시점, 주문/결제 상태 전이, 소유권 정책 변경
- 기존 공개 API 또는 저장 데이터와 호환되지 않는 변경
- 실제 결제 시스템, 운영 비밀정보, 외부 인프라 또는 배포 환경 연결
- 대규모 패키지 재구성이나 핵심 프레임워크 교체

## 검증 및 완료 기준

- 변경 범위에 맞는 테스트를 추가하거나 갱신한다.
- 최소한 `.\gradlew.bat test`를 실행하고, 빌드나 설정 변경 시 `.\gradlew.bat build`도 실행한다.
- 스키마 변경은 JPA 매핑, Flyway 제약, 관련 도메인 문서가 일치해야 한다.
- 정책이나 주요 흐름이 바뀌면 관련 `docs/` 문서와 필요 시 ADR을 같은 작업에서 갱신한다.
- 무관한 사용자 변경을 수정하거나 되돌리지 않는다.

## 완료 보고

작업 완료 시 다음을 간결하게 보고한다.

1. 변경한 동작과 주요 파일
2. 중요한 설계 판단 또는 보존한 불변조건
3. 실행한 검증 명령과 결과
4. 남은 제한, 위험, 후속 결정이 있으면 해당 내용
